# NotesApp-localStorage
 **Notes App(local Storage)** built with HTML, CSS, and JavaScript.   It uses **LocalStorage** to save notes directly in your browser so they remain even after refresh or closing the tab.  
